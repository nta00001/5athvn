
export interface GalleryImage {
  publicId: string;
  alt: string;
}

export interface TimelineEvent {
  date: string;
  title: string;
  description: string;
  publicId: string;
  imgAlt: string;
}
